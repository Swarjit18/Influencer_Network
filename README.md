# Influencer_Network
This program creates a browser-based Influencer Network using HTML, CSS, and PyScript. PyScript runs the Python logic natively to manage user inputs and network connections without a server, while CSS styles the grid menu and mock console with vibrant gradients.
